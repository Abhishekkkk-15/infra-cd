version: "1.0"
jobs:
  - name: "Build API Backend"
    script: |
      echo "Building Go API server..."
      go build -o api ./cmd/infra-cd/main.go
      
  - name: "Start API Server"
    background: true
    script: |
      echo "Starting API server..."
      sudo systemctl stop infra-cd-agent


